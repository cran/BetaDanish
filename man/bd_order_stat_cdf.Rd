% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structural.R
\name{bd_order_stat_cdf}
\alias{bd_order_stat_cdf}
\alias{bd_order_stat_moments}
\title{Order Statistic Distribution and Moments}
\usage{
bd_order_stat_cdf(t, i, n, a, b, c, k)

bd_order_stat_moments(r, i, n, a, b, c, k, rel.tol = 1e-10)
}
\arguments{
\item{t}{Vector of times, for `bd_order_stat_cdf`.}

\item{i}{Index of the order statistic, from 1 to `n`.}

\item{n}{Sample size.}

\item{a}{Shape parameter (beta generator).}

\item{b}{Shape parameter governing the tail.}

\item{c}{Shape parameter (baseline).}

\item{k}{Scale parameter (baseline).}

\item{r}{Order of the moment, for `bd_order_stat_moments`.}

\item{rel.tol}{Passed to [stats::integrate()].}
}
\value{
`bd_order_stat_cdf` returns a vector the length of `t`;
  `bd_order_stat_moments` returns a single number.
}
\description{
Order Statistic Distribution and Moments
}
\details{
\eqn{F_{i:n}(t) = I_{F(t)}(i, n - i + 1)}, and moments are taken against the
order-statistic density on the Beta scale. The \eqn{i}-th order statistic of
a sample of size \eqn{n} has a finite \eqn{r}-th moment when
\eqn{b(n - i + 1) > r}, so extremes need heavier restrictions than the parent
distribution, and the maximum needs \eqn{b > r} exactly as the parent does.
}
\examples{
bd_order_stat_cdf(c(0.5, 1, 2), i = 3, n = 5, a = 1.5, b = 3, c = 2, k = 1)
bd_order_stat_moments(1, i = 1, n = 5, a = 1.5, b = 3, c = 2, k = 1)
}
\seealso{
[bd_order_stat_pdf()]
}
