% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{bd_moments}
\alias{bd_moments}
\title{Raw Moments of the Beta-Danish Distribution}
\usage{
bd_moments(r, a, b, c, k, rel.tol = 1e-10, subdivisions = 4000L)
}
\arguments{
\item{r}{Order or orders of the moment. Non-negative.}

\item{a}{Shape parameter (beta generator).}

\item{b}{Shape parameter governing the tail; `E(Z^r)` is finite iff `b > r`.}

\item{c}{Shape parameter (baseline).}

\item{k}{Scale parameter (baseline).}

\item{rel.tol}{Relative accuracy, passed to [stats::integrate()].}

\item{subdivisions}{Subdivision limit, passed to [stats::integrate()].}
}
\value{
A numeric vector the length of `r`. Entries with `b <= r` are `Inf`.
}
\description{
Raw Moments of the Beta-Danish Distribution
}
\details{
The survival function is regularly varying with index \eqn{-b}, so
\eqn{E(Z^r)} is finite if and only if \eqn{b > r}. That condition is checked
rather than assumed: a request for a moment that does not exist returns
`Inf`, not a large finite number produced by a truncated sum.
}
\examples{
bd_moments(1:2, a = 1.5, b = 3, c = 2, k = 1)

# The fourth moment does not exist when b = 3
bd_moments(4, a = 1.5, b = 3, c = 2, k = 1)
}
\seealso{
[bd_moment_summary()], [bd_incomplete_moment()]
}
