% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_study.R
\name{simulate_bd_competing_data}
\alias{simulate_bd_competing_data}
\title{Simulate Competing Risks Data}
\usage{
simulate_bd_competing_data(
  n,
  pars = list(c(a = 1, b = 2.5, c = 1.8, k = 0.04), c(a = 1, b = 3.5, c = 1.2, k = 0.02)),
  gammas = NULL,
  censor_rate = 0.15,
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{pars}{List of parameter vectors, one per cause, each a named vector
with `b`, `c`, `k` and optionally `a`. Defaults to two causes.}

\item{gammas}{Numeric vector of accelerated failure time coefficients, one
per cause, acting on a single binary covariate. `NULL` for no covariate.}

\item{censor_rate}{Approximate proportion of right-censored observations.}

\item{seed}{Optional integer seed.}
}
\value{
A data frame with `time`, `cause` (0 for censored) and, when
  `gammas` is supplied, the covariate `x`.
}
\description{
Simulate Competing Risks Data
}
\examples{
d <- simulate_bd_competing_data(200, seed = 1)
table(d$cause)
}
\seealso{
[fit_bd_competing()]
}
