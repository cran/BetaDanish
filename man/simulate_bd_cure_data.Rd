% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_bd_cure_data}
\alias{simulate_bd_cure_data}
\title{Simulate Beta-Danish Cure Data}
\usage{
simulate_bd_cure_data(
  n,
  type = c("mixture", "promotion"),
  a = 1,
  b = 2,
  c = 1.5,
  delta,
  gamma,
  X,
  Z,
  target_censor = 0.3,
  seed = NULL
)
}
\arguments{
\item{n}{Integer; number of observations.}

\item{type}{Character; `"mixture"` or `"promotion"`.}

\item{a, b, c}{Numeric; baseline shape parameters.}

\item{delta}{Numeric vector; coefficients for the latency scale `k`.}

\item{gamma}{Numeric vector; coefficients for the incidence/cure component.}

\item{X}{Matrix; design matrix for latency (must match length of `delta`).}

\item{Z}{Matrix; design matrix for incidence (must match length of `gamma`).}

\item{target_censor}{Numeric; target proportion of censoring to calibrate the
exponential censoring rate. Default is 0.3.}

\item{seed}{Integer; optional seed.}
}
\value{
A list containing the simulated `data` (time, status), the `cured`
  indicator, and the true parameters.
}
\description{
Generates synthetic survival data from a Beta-Danish mixture or promotion-time
cure model, incorporating covariates.
}
