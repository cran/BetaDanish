% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aft_models.R
\name{fit_bd_aft}
\alias{fit_bd_aft}
\title{Fit Beta-Danish AFT Regression Model}
\usage{
fit_bd_aft(formula, data, n_starts = 10, method = "BFGS")
}
\arguments{
\item{formula}{A survival formula (e.g., `Surv(time, status) ~ age + treatment`).}

\item{data}{A data frame containing the variables.}

\item{n_starts}{Integer; number of random starts for optimization.}

\item{method}{Optimization method passed to `maxLik`.}
}
\value{
An object of class `bd_aft`.
}
\description{
Fits an Accelerated Failure Time (AFT) regression model using the
Exponentiated Danish (ED) kernel, that is the Beta-Danish distribution with a = 1.
}
\details{
To ensure identifiability, the shape parameter `a` is fixed to 1. The scale
parameter `k` is linked to covariates via `k_i = exp(X_i %*% delta)`.
Positive coefficients in `delta` indicate a larger `k`, which corresponds
to shorter survival times (accelerated failure).
}
