% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{bd_incomplete_moment}
\alias{bd_incomplete_moment}
\title{Incomplete Moments}
\usage{
bd_incomplete_moment(r, t, a, b, c, k, lower = TRUE, rel.tol = 1e-10)
}
\arguments{
\item{r}{Order of the moment.}

\item{t}{Vector of thresholds.}

\item{a}{Shape parameter (beta generator).}

\item{b}{Shape parameter governing the tail.}

\item{c}{Shape parameter (baseline).}

\item{k}{Scale parameter (baseline).}

\item{lower}{Logical; `TRUE` (default) for \eqn{M_r(t)}, `FALSE` for
\eqn{\Upsilon_r(t)}.}

\item{rel.tol}{Passed to [stats::integrate()].}
}
\value{
A numeric vector the length of `t`.
}
\description{
The lower incomplete moment \eqn{M_r(t) = \int_0^t z^r f(z)\,dz} and the
upper incomplete moment \eqn{\Upsilon_r(t) = \int_t^\infty z^r f(z)\,dz}.
}
\details{
The threshold is mapped to the Beta scale by \eqn{u_t = G(t)}, computed on
the log scale so it stays accurate when \eqn{G(t)} approaches one, and the
integral is taken over \eqn{(0, u_t)} or \eqn{(u_t, 1)}.
}
\examples{
a <- 1.5; b <- 3; c <- 2; k <- 1
t <- 1
# The two halves sum to the complete moment
bd_incomplete_moment(1, t, a, b, c, k, lower = TRUE) +
  bd_incomplete_moment(1, t, a, b, c, k, lower = FALSE)
bd_moments(1, a, b, c, k)
}
\seealso{
[bd_mrl()], [bd_lorenz()], [bd_mean_deviation()]
}
